# Override any value here to control the container at runtime.
# docker compose --env-file .env up (or put these in a local `.env` — compose auto-loads it).

MODEL_NAME=all-MiniLM-L6-v2
EMBED_DIM=384
FAISS_INDEX=wiki_faiss.index
TITLES_FILE=wiki_titles.txt
DEFAULT_K=5
DEFAULT_NPROBE=64
PORT=8000
WORKERS=1
